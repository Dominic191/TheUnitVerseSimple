namespace TheUnitVerseSimple
{
    // Gets the main navigation shell for the app
    public partial class AppShell : Shell
    {
        // Gets the constructor for the shell
        public AppShell()
        {
            // Initializes the shell components (this is defined in the XAML already)
            InitializeComponent();
        }
    }
}
